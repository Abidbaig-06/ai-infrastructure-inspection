/**
 * INFRASPECTION Internal Routing & Ecosystem Configuration
 * Configured internally without exposing raw URLs in the visible UI
 */

export const ROUTES = {
  HOME: '/',
  COMPLAINT_APP_URL: '/report-issue',
  INSPECTOR_WORKSPACE: '/workspace',
  TRACK_TICKET: '/track',
  ANALYTICS: '/analytics',
  MEE_BHOOMI_REGISTRY: '/land-registry'
};
